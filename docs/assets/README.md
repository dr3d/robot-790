# Static Page Assets

CSS and JavaScript for the GitHub Pages site live here.

- `site.css`: visual styling for the public memory palace.
- `site.js`: loads `catalog.json`, renders articles, logs, media, and the
  random banner line.

Keep this folder dependency-light so the page works as a simple static site on
GitHub Pages or from a local file server.

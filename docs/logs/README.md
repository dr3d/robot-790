# Public Transcript Logs

Drop curated public-safe conversation transcripts here as `.txt`, `.log`, or
Markdown files. The public page renders them in a monospaced transcript panel.

Keep raw private runs in `logs/` or `sessions/` until they have been curated.

After adding or removing public logs, rebuild the static catalog:

```powershell
.\scripts\build_docs_catalog.ps1
```
